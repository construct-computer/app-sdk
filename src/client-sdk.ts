/**
 * Client-side SDK assets served by `ConstructApp` at `/sdk/construct.js` and
 * `/sdk/construct.css`.
 *
 * The editable source files live in `assets/construct.js` and
 * `assets/construct.css`. `pnpm build` regenerates the typed string exports in
 * `src/generated/client-sdk-assets.ts` before running `tsc`.
 */

export { CONSTRUCT_SDK_CSS, CONSTRUCT_SDK_JS } from './generated/client-sdk-assets.js';
